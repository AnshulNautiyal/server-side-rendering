import React from "react";

export default () => {
  return (
    <div>
      This is home page in github cloudspace
      <button onClick={() => console.log("button clicked")}>Click</button>
    </div>
  );
};
